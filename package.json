{
  "scripts": {
    "dev": "browser-sync start --server src --files '*.html, css/*.css, js/*.js'",
    "serve": "npx json-server src/app/data/users.json"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "json-server": "^1.0.0-beta.15"
  }
}
